extends templates/main
block variables
	- let pageTitle = 'Liarch - Architecture & Interior HTML Template | Works Grid';
	- let isHome = false; //- Если true то логотип не будет ссылкой
	- let isDark = false;
	- let currentPage = 3.01; // Для каждой страницы уникален. Номер смотреть в файле pug/templates/_vars.pug в массивах с пунктами меню
	- let currentSection = 3; // // Для каждой раздела уникален. Home -1, about - 2, works - 3, news - 4, pages - 5

block header
	include modules/header
	include modules/menu
block content
	article.works-page
		header.works-page__header
			.works-page__inner.container
				h1.works-page__heading.heading Architecture is a visual art, and&nbsp;the&nbsp;building speak for themeselves
				.works-page__text Craft beer elit seitan exercitation, photo booth et 8-bit kale chips proident chillwave deep laborum. Aliquip veniam delectus, Marfa eiusmod Pinterest in do umami readymade swag. Self iPhone Kickstarter, drinking vinegar jean vinegar stumptown yr pop-up artisan.
		.works-page__content
			.works-page__inner.container

				// Works
				section.works
					header.works__header

						// Filter
						.works__filter.filter
							each item in ['all', 'architecture', 'interior', 'cgi', 'products']
								- let filterVal = item === 'all' ? '*' : '.__js_' + item;
								- let activeClass = item === 'all' ? ' filter__item--active' : '';
								button(class="filter__item" + activeClass, type="button", data-filter= filterVal)= item

					// Works list
					ul.works__list.row.__js_works-filter
						+work-card('cube-villa-poland.jpg', 'Architecture, Interior', 'Cube villa, Poland', 'Discover how we helped CleanCloud to increase their presence on a global market by their scaling up its visual identity.')(class="__js_architecture __js_interior")

						+work-card('stone-house.jpg', 'Architecture, CGI', 'Stone house', 'Stone House is a digital platform that provides university students with personalized learning pathways')(class="__js_architecture __js_cgi")

						+work-card('single-house.jpg', 'CGI, Interior', 'Single house', 'Discover how we helped them scale up from local to global.')(class="__js_cgi __js_interior")

						+work-card('daniel-villa-romania.jpg', 'Architecture', 'Daniel villa, Romania', 'Had hath and bearing had night, had own, second you\'ll said deep very said firmament made living appear')(class="__js_architecture")

						+work-card('cabin-wood-in-forest.jpg', 'CGI, Product', 'Cabin wood in forest', 'Fill lesser dominion moveth is appear had, lesser.')(class="__js_cgi __js_product")

						+work-card('b65-apartment-italy.jpg', 'Interior', 'B65 apartment, Italy', 'So creepeth image greater signs great us together.')(class="__js_interior")

						+work-card('ODE-kid-garden-salburg.jpg', 'Architecture, CGI, Products', 'ODE kid garden, salburg', 'Deep dry let man without first air made divide divide without')(class="__js_architecture __js_cgi __js_products")

						+work-card('grido-museum-denmark.jpg', 'Architecture, Interior', 'Grido museum, Denmark', 'Sea seasons. Had hath and bearing had night, had own, second you\'ll said deep very said firmament made living appear')(class="__js_architecture __js_interior")

						+work-card('dogel-nord-station.jpg', 'Architecture, Interior', 'Dogel nord station', 'Own appear fifth, very god made darkness without which whose meat fill, over sixth of which Good in third gathering')(class="__js_architecture __js_interior")

					// Button more
					a.works__more.more-btn(href="#")
						span.more-btn__text more

block footer
	include modules/footer
